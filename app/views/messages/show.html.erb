
<p><strong>From:</strong> <%= @message.sender == @user ? link_to("You", user_path(@user)) : link_to(h(@message.sender.name), user_path(@message.sender)) %></p>
<p><strong>Received:</strong> <%= @message.created_at.to_s(:long) %></p>
<p><strong>To:</strong> <%= @message.recipient == @user ? link_to("You", user_path(@user)) : link_to(h(@message.recipient.name), user_path(@message.recipient)) %></p>
<p>
  <strong>Message</strong><br />
  <%=h @message.body %>
</p>
<p>
  <% if @message.recipient == @user %>
	  <%= link_to "Reply", new_user_message_path(@user, :reply_to => @message, :rec => @message.recipient) %> |
  <% end %>
  <%= link_to "Inbox", user_messages_path(@user)%>
</p>
